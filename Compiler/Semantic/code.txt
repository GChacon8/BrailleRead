// Test parser

Proc @Master
(
    New @variable1, (Num, 1);
    Until (
        PrintValues("El valor de '@variable1' es: ", @variable1);
        Signal(3, 1);
        Values(@variable1, Alter(@variable1, ADD, 1));
    ) @variable1 > 10;
);