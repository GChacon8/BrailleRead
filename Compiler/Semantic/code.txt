// Test1

// Procedimiento Master
Proc @Master
(
    //A
    Signal(3, 1);
    EndSignal();
    Call(@abc);
    New @var1, (Num, 23);
    New @var2, (Num, (@var1 SUB 3) DIV 5);
    PrintValues("El valor de '@var2' es: ", @var2);
);

// Procedimieto abc
Proc @abc(
    // Creacion de variable
    New @var1, (Num, 28);
    Signal(6, 1);
);